apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: backend-storage-pvc
spec:
    storageClassName: std-storage
    resources:
        requests:
            storage: 10Gi
    accessModes:
        - ReadWriteOnce
