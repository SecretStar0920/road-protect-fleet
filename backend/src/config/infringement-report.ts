export const infringementReportConfig = {
    // Infringement reports are scheduled to be sent out at 9am (UTC) every Sunday
    accountCronFrequency: '0 0 9 * * 0',
    // Infringement reports are scheduled to be sent out at 9am (UTC) every Sunday
    accountRelationCronFrequency: '0 0 9 * * 0',
    // unmanaged accounts
    unmanagedCronFrequency: '0 0 9 * * 0',
    
    reportingPeriodInDays: 7,
};
